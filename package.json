{
  "name": "openapi-gen-bug",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "gen": "rimraf output-6.0.1 && openapi-generator-cli generate"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.5.1",
    "rimraf": "^3.0.2"
  }
}
